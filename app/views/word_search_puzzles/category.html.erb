<% provide(:title, "#{@category.link_to_category} search word puzzles") %>
<% provide(:page_type, 'page') %>
<% provide(:meta_description, "#{@puzzles.count} free, printable word search #{"puzzle".pluralize(@puzzles.count)} available for #{@category.link_to_category} for ESL teachers.") %>
<% provide(:meta_keywords, "#{@category.link_to_category}, esl, teaching activity, tefl, search word creator, word search creator, word find creator, search word puzzle, word search puzzle, word find puzzle, teaching, activities, teaching activity, printable, print, printing") %>
<% content_for :mixpanel_tracking do %>
	<script type="text/javascript">
  	  $(document).ready( function(){
        mixpanel.track("Seeing Category Page", { 'category': "<%= @category.capitalize %>"});
      });
  </script>
<% end %>
<% content_for :sidebar do %>
	<div id="sidebar" class="col-side left">
		<div class="widget">
			<h2>Get Started!</h2>
			<ul>
				<li>
					<p>
						Start using Search Word Creator to Create your own <strong>printable search word puzzles.</strong>
					</p>
				</li>
			</ul>
		</div>
	</div>
<% end %>

<h1><%= @category.link_to_category.titlecase %> Search Word Puzzles</h1>
<p>Below are the <strong>word search puzzles</strong> that are available for <%= @category.link_to_category.titlecase %>.  Click on any puzzle to get started <strong>creating a custom, printable search word puzzle</strong> from this word list within 15 seconds! </p>
<br/>
<div class="alert alert-info">
	<h3 style="text-align: center;">We have <%= pluralize(@puzzle_count, "puzzle") %> in <%= pluralize(@puzzles_category_count, "category") %>!</h3>
	<br/>
	<p>Don't like anything in this category?  Go to the <%= link_to "main index", word_search_puzzles_path %> to see all <%= pluralize(@puzzles_category_count, "category") %> from which you can choose!</p>
	<br/>
</div>

<div class="promo-row">
<% @puzzles.each_with_index do |puzzle, index| %>
	<% if index % 3 == 0 && index != 0 %>
	</div>
	<div class="promo-row">
	<% end %>
	<%= link_to puzzle.name, promo_puzzle_path(@category, puzzle.name.category_to_link ) %>
<% end %>
</div>